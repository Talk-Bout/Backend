// import { prisma } from '../../Infrastructures/utils/prisma'
// import { EmailValidator } from '../validators'

// export default (email: EmailValidator) => {
//   const User = prisma.user
//   return User.findUnique({ where: email })
// }
